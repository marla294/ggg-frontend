import styled from 'styled-components';

const ListStyles = styled.div`
  display: grid;
  grid-template-areas: 'a';
  gap: 1rem;
  grid-auto-columns: minmax(10rem, 100%);
`;

export default ListStyles;
